# ITPM_Assignment1_Option1

# Install required packages
  # pip install -U pip 
  # pip install playwright openpyxl 
  # playwright install 

# This project tests the accuracy of the Singlish → Sinhala translator available at:
  https://www.pixelssuite.com/chat-translator

# It includes 50 designed FAIL test cases.

# Run the automation script using:
  python test_automation.py --excel "test_automation/Assignment 1 - Test cases.xlsx" --url "https://www.pixelssuite.com/chat-translator" --wait-ms 5000 --type-delay-   ms 80 --slow-mo-ms 200 --save-every 1 --keep-open


